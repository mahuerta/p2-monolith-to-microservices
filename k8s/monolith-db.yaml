apiVersion: apps/v1 
kind: Deployment
metadata:
  name: monolith-db
spec:
  selector:
    matchLabels:
      app: monolith-db
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: monolith-db
    spec:
      containers:
      - image: mysql:5.7
        name: mysql
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: password
        - name: MYSQL_DATABASE
          value: library-db
        ports:
        - containerPort: 3306
          name: mysql
---
apiVersion: v1
kind: Service
metadata:
  name: monolith-db
spec:
  type: ClusterIP
  selector:
    app: monolith-db
  ports:
  - port: 3306